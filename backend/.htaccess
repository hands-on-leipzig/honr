# If the server's DocumentRoot is set to backend/ instead of backend/public/,
# this redirects all requests to the public directory.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
